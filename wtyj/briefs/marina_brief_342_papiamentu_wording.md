# BRIEF342 — Correct Curaçao Papiamentu wording
**Status:** Independent brief, exact-copy plan and output review approved; implementation ready for candidate validation | **Files:** `clients/mermaid/config/client.json`, `clients/mermaid/config/reservation_catalog.json`, `clients/mermaid/config/response_policy.json`, Papiamentu-only display constants in `wtyj/agents/social/mermaid_reservation_workflow.py`, `wtyj/agents/social/mermaid_documents.py`, `wtyj/agents/social/mermaid_model_recovery.py`; existing focused test files | **Depends on:** deployed aebe37f | **Blocks:** correction of remaining recorded Papiamentu wording defects

## Context
The completed 6714e33 model audit preserved malformed and mixed generated phrases such as “Ku muhcto gustu”, “Para kual fecha”, “refreshment/Beer”, “Ser bienbenido pa bebe serbes o bino” and unclear child/swimwear/cash nouns. These are attested historical findings in the final NL/PAP review, not invented reproductions. The newline defect is already repaired in aebe37f. Current generated text sees only nine critical glossary entries plus English supplementary facts; catalog guest_copy is removed from the model context. The Papiamentu prompt asks for supplied weekday spellings but does not supply the policy weekday list. Existing fixed display copy also has inconsistent trip/transporte terminology, Dutch “over”, unaccented Wednesday and inconsistent lodging spelling.

The official Curaçao government publication of the FPI word list supports spellings including almuerso, desayuno, refresko, djus, alohamentu, outo, snòrkel, reservashon and turtuga. Its preface allows attested variants; a loanword alone is not an error. A spelling reference establishes orthography, not a human reviewer’s approval of new sentences. No native reviewer identity or signed approval was found; retain that provenance honestly.

## Why This Approach
Expand the existing configuration-owned glossary with supported vocabulary, safe short phrasing examples and the existing configured weekday spellings. Append targeted Curaçao guidance to the already-injected language_register while preserving the German guidance exactly. Correct only existing Papiamentu display-copy leaves/constants and preserve every placeholder, price, age band, time and operational meaning. Reject Python replacement/classification of generated language, extra model calls per guest turn, memorized answers for the audit and fabricated native approval: they would hide the underlying language weakness or change established behavior. New sentences remain reference-checked assistant translations, which a focused fresh synthetic run can evaluate without contacting guests.

## Instructions
1. Complete a source-backed exact before/after copy plan and have it independently reviewed before edits. Record cited spellings and contextual meaning evidence separately from assistant phrasing decisions. The owner explicitly requires standard written Curaçao Papiamentu, with correct spelling, accents and grammar and a polite, professional register. Exclude slang, phonetic abbreviations and unnecessary language mixing from assistant output. Valid established loanwords remain acceptable. Use plain, short sentences; preserve proper names and exact guest-provided evidence.
2. Extend `response_policy.json` glossary and append the Papiamentu register paragraph at `client.json:60`, using the existing glossary and register injection at `mermaid_understanding.py:118` and `:125`. Include weekday labels from the existing policy, age-band wording without confusing fare groups with adulthood, food/drink vocabulary, cash and swimwear, pickup/return and truthful queued-review language. Keep the native-review field pending; add separate reference-review evidence in documentation. No new runtime routing or classifier.
3. Apply the reviewed Papiamentu-only catalog copy edits. Update existing Papiamentu workflow constants near `mermaid_reservation_workflow.py:96`, `:136`, `:146`, `:156`, document constants near `mermaid_documents.py:53` and `:114`, and recovery HUMAN_COPY near `mermaid_model_recovery.py:31` only where the copy plan requires it. Preserve all other locales, German register, configured rates/times, safety/dietary limitations, snapshots, payment/cancellation state, controls, provider settings and owner contacts. Existing demo disclosures remain clear.
4. Render a synthetic Papiamentu quote/receipt after corrections; check readable text, no overflow, unchanged amounts and demo notices. Preserve the original and five prior audit ledgers/transcripts rather than rewriting their raw scores.
5. Run one focused real-model synthetic validation of the original ten Papiamentu cases, the same Papiamentu paraphrase and the original English-to-Papiamentu switch case, with separate denominators. Retain exact SDK captures, one-call-per-turn behavior, saved state/documents and independent full transcript review. Carry the existing estimated USD31.395849/1,036 calls and enforce a new incremental maximum USD4, at most80 calls, no resume/reset. No real guest messages, payments, new monitors or unrelated tenant restart.
6. Build a pinned image over the verified aebe37f runtime, overlaying only reviewed changed modules and bundled policy. Deploy only Mermaid using per-leaf configuration guards, private backup, graceful stop/recreate and tested rollback. Verify the actual image/files, config scope, active/available controls, peers, maintenance removal and public/health endpoints. Keep the Codex monitor paused.

## Tests
Extend existing tests with five meaningful behaviors: actual SDK request receives the configured Curaçao guidance/glossary/weekdays in ordinary and review contexts while preserving exact guest facts; phone/summary/quote flow uses corrected Papiamentu and retains its required-contact/one-approval rules; five-passenger pickup renders unchanged USD75 return coverage and fixed wording; queued review plus FAQ still answers while preserving the freeze, and explicit human fallback uses corrected text without a model; generated Papiamentu quote/receipt has corrected text, the same totals/age bands and prominent demo disclosures. Run the relevant existing recovery, catalog, workflow, document and public-projection suites. Mocked SDK output proves plumbing only; fresh-run transcript evidence is evaluated separately and cannot constitute native-human certification.

## Success Condition
Reference-backed Curaçao wording and the full weekday vocabulary reach Tracy’s generated replies and fixed documents without changing business or handover behavior; fresh sampled conversations and deployment are verified and their limits recorded honestly.

## Rollback
Restore the pinned aebe37f image and only the changed live config/compose bytes from the guarded private backup, preserving conversation data and intentional controls; source rollback reverts the single wording commit. Do not replay guest messages or alter prior audit evidence.
